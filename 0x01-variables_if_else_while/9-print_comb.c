#include <stdio.h>
/**
 * main - program prints all possible combinations of single-digit numbers putting space and comma.
 *
 * Return: 0
 */
int main(void)
{
int b;

for (b = 18; b <= 27; c++)
{
putchar(b);
if (b != 27)
{
 putchar(',');
 putchar(' ');
}
}
putchar('\n');
return (0);
}
